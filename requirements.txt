django-cors-headers==4.9.0
fastapi==0.136.3

uvicorn[standard]==0.27.0
pandas==2.1.4
numpy==1.26.3
pydantic>=2.9.0
python-multipart==0.0.6



# ── Web Framework ─────────────────────────────────────────────────────────────
Django==5.1.7
daphne==4.2.1           # ASGI server (required for Django async / WebSocket support)
asgiref==3.11.0         # Django's async bridge (sync_to_async used in FastAPI layer)

# ── Template & Form Utilities ─────────────────────────────────────────────────
django-crispy-forms==2.5
crispy-bootstrap5==2025.6

# ── FastAPI (REST API layer) ───────────────────────────────────────────────────
starlette==1.2.1        # FastAPI foundation (CORS middleware imported directly)        # Request / response schema validation      # ASGI server for running the app in production

# ── DHS Dataset Processing ────────────────────────────────────────────────────
pyreadstat==1.3.5       # Read Stata .DTA files        # DataFrame operations for indicator computation          # Numerical helpers used in indicator functions

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.2.1    # Load environment variables from a .env file
requests==2.31.0